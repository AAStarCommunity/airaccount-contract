[profile.default]
src = "src"
test = "test"
script = "script"
out = "out"
libs = ["lib"]

solc_version = "0.8.33"
evm_version = "cancun"
via_ir = true
optimizer = true
optimizer_runs = 1    # minimize deploy-time bytecode size (account 26KB+ needs EIP-170 headroom after clone refactor)

remappings = [
    "@account-abstraction/=lib/account-abstraction/contracts/",
    "@openzeppelin/=lib/openzeppelin-contracts/",
    "forge-std/=lib/forge-std/src/",
]
